# =============================================================================
# NCE — Development & CI Dependencies (pinned for reproducible checks)
# Last reviewed: 2026-06-05
#
# Update policy: bump pins here when CI upgrades are intentional.
# Run `pip install -r requirements-dev.txt` to refresh local dev environment.
# =============================================================================

ruff==0.15.16
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-mock==3.15.1
pytest-httpx==0.36.2
pip-tools==7.4.1
pip-audit==2.9.0
